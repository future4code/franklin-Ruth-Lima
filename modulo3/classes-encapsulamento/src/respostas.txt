/// Exercicio 1

a) O construtor dentro de uma classe serve como um inicializador, ele recebe parametros e referenciam os atributos contidos na classe.
 ele é chamdo atraves de uma variavel que passa um new + nome da classe (passando o que ela quer dentro desse parenteses) e a baixo fazendo
 a chamada dele nome.atributo.


b) apareceu a frase do console somente uma vez no terminal. 

c) conseguimos ter acesso as propriedades privadas apartir dos metodos getters e setters.